{
  "name": "unified-peers-mcp",
  "version": "0.1.0",
  "description": "Cross-runtime peer messaging broker. Hosts a single localhost daemon and CLI shared by the opencode and claude runtimes (and any future agent runtime).",
  "type": "module",
  "private": true,
  "scripts": {
    "broker": "bun broker.ts",
    "diagnose": "bun cli.ts diagnose",
    "test": "bun test"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.14.48",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  }
}
